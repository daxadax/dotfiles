syntax enable

" use vim settings rather than vi
set nocompatible

" set size
set textwidth=80

" expand tabs to spaces, default two
set expandtab
set tabstop=2

" show line numbers
set number  

"         shortcuts          "

" show matching brackets
set showmatch
