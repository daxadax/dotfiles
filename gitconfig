[color]	
ui = true
[push]
default = simple
[alias]
nuke = !sh -c 'git branch -d $1 && git push origin :$1' -
prettylog = log --graph --abbrev-commit --decorate --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white) - %an%C(reset)' --all
[credential]
	helper = cache --timeout=28800
[user]
	name = dax
